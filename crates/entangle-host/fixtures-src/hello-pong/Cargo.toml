[workspace]
[package]
name = "hello-pong"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["cdylib"]

[dependencies]
entangle-sdk = { path = "../../../entangle-sdk" }

[profile.release]
opt-level = "s"
lto = true
codegen-units = 1
strip = true
